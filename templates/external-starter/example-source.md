# External starter example

This example is safe to ingest and contains no private data.

An LLM-Wiki keeps original source files unchanged. It creates traceable wiki pages that separate extracted facts from inference and synthesis. Agent-generated pages start as drafts and require human review. A public export contains only approved public pages and runs a redaction preview before release.

The human reviewer owns the final interpretation and publication decision.
