# Agent adapter docs

These docs describe how to use the LLM-Wiki Skills pack with different coding agents.

| Agent | Doc |
|---|---|
| Claude Code | `claude-code.md` |
| Codex | `codex.md` |
| Cursor | `cursor.md` |
| OpenCode | `opencode.md` |

The common pattern is:

```text
install skills -> keep instruction files short -> point agents at wiki/index.md -> use git diffs for review
```
